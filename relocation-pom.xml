<?xml version="1.0" encoding="UTF-8"?>
<!--
  Relocation POM for the old coordinate.

  Publish this as a standalone artifact (no JAR) to Maven Central so existing
  users of `io.github.enesdurmus:spring-boot-starter-mqtt` are automatically
  redirected to the new coordinate `io.github.enesdurmus:mqtt-spring-boot-starter`.

  How to publish (one-time):
    mvn -f relocation-pom.xml deploy -P release
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>io.github.enesdurmus</groupId>
    <artifactId>spring-boot-starter-mqtt</artifactId>
    <version>1.0.3</version>
    <packaging>pom</packaging>

    <name>spring-boot-starter-mqtt (relocated)</name>
    <description>This artifact has been renamed to mqtt-spring-boot-starter.</description>
    <url>https://github.com/enesdurmus/mqtt-spring-boot-starter</url>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <developers>
        <developer>
            <id>enes-durmus</id>
            <name>Enes Durmus</name>
            <email>enesdurmus3738@gmail.com</email>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:git://github.com/enesdurmus/mqtt-spring-boot-starter.git</connection>
        <developerConnection>scm:git:ssh://github.com:enesdurmus/mqtt-spring-boot-starter.git</developerConnection>
        <url>https://github.com/enesdurmus/mqtt-spring-boot-starter</url>
    </scm>

    <distributionManagement>
        <relocation>
            <groupId>io.github.enesdurmus</groupId>
            <artifactId>mqtt-spring-boot-starter</artifactId>
            <version>1.1.0</version>
            <message>Renamed to follow the community convention &lt;name&gt;-spring-boot-starter.</message>
        </relocation>
    </distributionManagement>

    <profiles>
        <profile>
            <id>release</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.sonatype.central</groupId>
                        <artifactId>central-publishing-maven-plugin</artifactId>
                        <version>0.9.0</version>
                        <extensions>true</extensions>
                        <configuration>
                            <publishingServerId>central</publishingServerId>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                        <version>3.2.7</version>
                        <executions>
                            <execution>
                                <id>sign-artifacts</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>sign</goal>
                                </goals>
                            </execution>
                        </executions>
                        <configuration>
                            <gpgArguments>
                                <arg>--pinentry-mode</arg>
                                <arg>loopback</arg>
                            </gpgArguments>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
